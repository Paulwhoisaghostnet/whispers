# Database (required for API). Use a serverless-friendly Postgres (e.g. Neon, Supabase).
DATABASE_URL=postgresql://user:password@host:5432/dbname?sslmode=require

# Optional: for extension build, point to your deployed API (e.g. Netlify site URL).
# VITE_API_BASE_URL=https://your-site.netlify.app
